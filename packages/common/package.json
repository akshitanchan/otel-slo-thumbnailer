{
  "name": "@thumbnailer/common",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared utilities for thumbnailer services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup src/index.ts --format esm --target es2022 --sourcemap --dts --watch",
    "build": "tsup src/index.ts --format esm --target es2022 --sourcemap --dts",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('no tests yet')\""
  },
  "dependencies": {
    "pg": "^8.13.1",
    "@opentelemetry/api": "1.9.0",
    "@opentelemetry/auto-instrumentations-node": "0.67.3",
    "@opentelemetry/exporter-trace-otlp-http": "0.208.0",
    "@opentelemetry/sdk-node": "0.208.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.10.9"
  }
}
